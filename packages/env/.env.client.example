# ─── Client Environment (Vite, React, Frontend) ─────────────────────────────
# Copy to apps/web/.env or your Vite app root
# Only VITE_* variables are exposed to the browser.

# API base URL for frontend requests
VITE_API_URL=http://localhost:3000
